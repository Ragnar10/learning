// Styles
import Styles from './styles.module.scss';

export const Banner = () => {
    return (
        <div className = { Styles.container }>
            <div className = { Styles.banner } />
        </div>
    );
};
